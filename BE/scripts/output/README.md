# Script Output

This folder stores saved outputs from helper scripts such as bulk re-embedding runs.

Recommended filename pattern:

- `reembed_<scope>_<mode>_<yyyy-mm-dd_hh-mm-ss>.json`
- `reembed_<scope>_<mode>_<yyyy-mm-dd_hh-mm-ss>.md`
